import { PLUGIN_FEATURES_PATH, PLUGIN_NAME } from '../../constants';

const feature = '/feature-one';

export const FEATURE_PATH = PLUGIN_FEATURES_PATH + feature;
export const FEATURE_REDUCER_KEY = PLUGIN_NAME + feature;
