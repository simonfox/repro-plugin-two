import { PLUGIN_FEATURES_PATH, PLUGIN_NAME } from '../../constants';
var feature = '/feature-one';
export var FEATURE_PATH = PLUGIN_FEATURES_PATH + feature;
export var FEATURE_REDUCER_KEY = PLUGIN_NAME + feature;
